# ==========================================
# 環境變數範本 - Daytona Park 跨境電商平台
# ==========================================
# 複製此檔案到 .env 並填入實際值
# Version: v4.0 (Scrapbook Retro Platform)

# ---- Database (必填) ----
# MongoDB Atlas 連接字串
DATABASE_URL=mongodb+srv://user:password@cluster.mongodb.net/daytona-park

# ---- Payload CMS (必填) ----
# 至少 32 字元的隨機密鑰，用於加密
PAYLOAD_SECRET=your-secret-key-at-least-32-characters-long

# ---- Server URL (必填) ----
# 本地開發：http://localhost:3000
# 生產環境：https://your-domain.com
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# ---- S3/R2 Storage (生產環境必填) ----
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=auto
S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# ---- Redis (生產環境建議啟用) ----
# 用於快取與 Rate Limiting
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=

# ---- LINE Messaging API (選填) ----
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# ---- Stripe (選填) ----
# 用於付款處理
STRIPE_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SIGNING_SECRET=whsec_xxx

# ---- Preview Mode (選填) ----
PREVIEW_SECRET=demo-draft-secret

# ---- Google Analytics 4 (選填) ----
# GA4 Property ID (格式: 123456789)
GA4_PROPERTY_ID=
# Service Account JSON (生產環境建議使用環境變數)
# 將整個 JSON 檔案內容作為字串存入
GA4_SERVICE_ACCOUNT_JSON=

# ---- Meta Conversions API (選填) ----
# Meta Pixel ID (用於伺服器端事件追蹤)
META_PIXEL_ID=894614306245367
# Meta CAPI Access Token (從 Meta Business Suite 取得)
META_CAPI_ACCESS_TOKEN=

# ---- Cron Job (選填) ----
# 用於驗證 Cron Job 請求來源
CRON_SECRET=your-cron-secret-key

# ---- Seed Control (開發用) ----
# 設為 true 時允許執行 /api/seed 端點
# 生產環境請勿設定此變數，以防止意外覆蓋資料
ENABLE_SEED=true

# ---- Environment ----
NODE_ENV=development
