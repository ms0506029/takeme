# Progress Tracker
**Last Updated：** 2026-01-30 (Admin SCSS 樣式系統完成)

---

## ✅ 已完成功能

### 核心基礎
- [x] Memory Bank 初始化
- [x] Payload 參考資料分析
- [x] 專案初始化 (Next.js 15 + Payload 3.x)

### Scrapbook v4.0 設計系統
- [x] 基於 `#C9915D` 的主題色系導入
- [x] Scrapbook 六大區塊開發 (Hero, News, Ranking, etc.)
- [x] 前後端資料接通與渲染邏輯
- [x] 自動化首頁生成指令 (`seed:scrapbook`)

### Admin UI 客製化 (Phase 6)
- [x] 主題色與 Noto Sans TC 字體套用
- [x] 側邊欄分類與圖示重整
- [x] Dashboard 統計卡片與歡迎區塊
- [x] 遺棄購物車欄位擴充

### 分析報表與第三方整合 (Phase 7)
- [x] GA4 與 Meta Pixel 追蹤碼整合
- [x] 追蹤碼後台管理 Global

### LINE Bot 整合 (Phase 7.5)
- [x] LINE Webhook 端點
- [x] Flex Message 模板 (5 種)
- [x] 會員綁定流程
- [x] 願望清單/補貨通知 Collection

### 會員中心 MyPage (Phase 8)
- [x] **Phase 8.1**: 會員卡片 + AccountNav 重構
- [x] **Phase 8.2**: 訂單履歷 + 點數履歷頁面
- [x] **Phase 8.3**: 收藏清單 + 補貨通知頁面
- [x] **Phase 8.4**: 基本資料 + 社群綁定 + 安全設定
- [x] **Vibe Polish**: Scrapbook Retro 視覺風格升級

### 購物車系統 (Phase 9) ✅
- [x] **Phase 9.1**: 後台設定擴充（SiteSettings 新增購物車設定）
- [x] **Phase 9.2**: Header Icon 整合 + CartDrawer 重構
- [x] **Phase 9.3**: 購物車專頁 `/cart`
- [x] **Phase 9.4**: 結帳頁面視覺優化

### EasyStore 商品匯入系統 ✅
- [x] SSE 串流即時進度推送
- [x] **完整變體同步**:
  - 自動建立 VariantTypes (顏色、尺寸)
  - 自動建立 VariantOptions (白色、FREE、S、M 等)
  - 自動建立 Variants (含價格、庫存、SKU)
- [x] **圖片-變體關聯**: 自動將圖片關聯到對應的變體選項
- [x] 進度介面顯示變體同步數量
- [x] Slug 生成修復（移除中文字符）
- [x] **幣別與匯率設定** (2026-01-28):
  - SiteSettings 新增「幣別與匯率設定」區塊
  - EasyStore 來源幣別設定（預設 TWD）
  - 網站預設幣別設定（預設 TWD）
  - 可選啟用匯率轉換
  - 自定義匯率列表（支援 USD、JPY、CNY、HKD）
  - 同幣別時自動跳過轉換，價格保持一致

### 商品頁整合 (Phase 11) 🚧
- [x] **WishlistButton 組件**: 愛心收藏按鈕
  - 登入狀態檢查
  - 收藏狀態切換（加入/移除）
  - 變體規格支援
- [x] **RestockNotifyButton 組件**: 補貨通知按鈕
  - 缺貨時自動顯示
  - 申請/取消通知功能
  - 變體規格支援
- [x] **VariantMatrix 組件**: 變體矩陣佈局（參考 Daytona Park）
  - 按顏色分組顯示所有尺寸
  - 每個尺寸有獨立收藏按鈕
  - 缺貨只標示在尺寸層級
- [x] **VariantWishlistButton 組件**: 行內小型愛心按鈕
- [x] **Gallery 優化**: 查看更多功能
- [x] **Cloudflare R2 圖片儲存配置** ✅
  - R2 Bucket: `takemejapan-media` (APAC 區域)
  - 公開 URL 已啟用: `pub-0253cb3d5f6644fc95fad66f6d98c09e.r2.dev`
  - CORS 政策已配置（localhost + 生產域名）
  - Payload CMS 儲存插件已整合
- [ ] 重新匯入商品圖片（使用 R2 儲存）
- [ ] 遺棄購物車 LINE 提醒（待實作）

### 爬蟲匯入系統修復 (2026-01-29) ✅
**問題診斷**：驗證 10 個已匯入的 Daytona Park 商品，發現嚴重資料缺失
- ❌ 商品名稱未翻譯（仍為日文/英文）
- ❌ 圖片未下載（imageUrls 空陣列）
- ❌ 價格為 0（originalPrice 未儲存）
- ❌ 變體顏色為日文（未翻譯為中文）
- ❌ SKU 未自動生成

**根因分析**：
1. `/api/scraper/products` 的 `formatProductData` 欄位名稱與 ScrapedProducts collection 不匹配
2. 平台選擇器配置與 Daytona Park 實際 HTML 結構不匹配
3. 匯入流程缺少翻譯和 SKU 生成邏輯

**修復內容**：
- [x] **Task 1**: 更新 `platform-seeds.ts` Daytona Park 選擇器
  - 標題: `h1.block-goods-name--text`
  - 價格: `.block-goods-price--price`
  - 品牌: `.block-goods-brand-area a`
  - 圖片: `.swiper-slide img`
  - 顏色變體: `.block-goods-color-variation-name`
  - 尺寸變體: `.block-goods-color-variation-size-value`
- [x] **Task 2**: 實作商品翻譯功能
  - 服飾類型翻譯（TRACK PANTS → 運動長褲）
  - 特徵翻譯（FIRE BIRD → 火鳥系列）
  - 台灣電商命名習慣轉換
- [x] **Task 3**: 實作 SKU 自動生成
  - 格式: `DP-{externalId}-{colorCode}-{size}`
  - 顏色代碼對照表（BK=黑色, NV=海軍藍, etc.）
- [x] **Task 4**: 修復 `/api/scraper/products` 欄位映射
  - 修正 `imageUrls` 結構（url + colorName）
  - 修正價格欄位為直接欄位（非巢狀 pricing.originalPrice）
  - 加入顏色翻譯（日文 → 中文）

### 商品匯入腳本完成 (2026-01-29) ✅
**需求**：建立測試腳本用於匯入爬取商品，包含圖片下載、變體建立、標題翻譯

**建立的腳本**：
- `scripts/create-test-scraping-job.ts` - 建立爬取任務和爬取商品測試數據
- `scripts/import-scraped-products.ts` - 匯入爬取商品到 Products Collection
- `scripts/cleanup-test-data.ts` - 清理測試數據（刪除已匯入商品、重置狀態）
- `scripts/delete-all-scraped-products.ts` - 刪除所有爬取商品

**匯入腳本功能**：
- [x] **圖片下載與上傳** - 從遠端 URL 下載圖片並上傳至 R2 Media Collection
- [x] **變體正確建立** - 使用 `getOrCreateVariantType()` 和 `getOrCreateVariantOption()` 建立正確的資料關聯
- [x] **顏色翻譯** - 日文顏色 → 中文顯示名稱（ブラック→黑色、ベージュ→奶茶、チャコールグレー→鐵灰等）
- [x] **標題翻譯** - 基於關鍵字的日文標題翻譯（スリム→修身、ワイド→寬版、テーパード→錐形等）
- [x] **尺寸表解析** - 將尺寸表 JSON 格式化為可讀文字
- [x] **商品描述生成** - 固定模板 + 動態尺寸表
- [x] **SKU 生成** - Freak Store 格式 `FS-{hash4}-{colorCode}-{size}`
- [x] **庫存狀態轉換** - 日文庫存狀態 → 數量（残りわずか→0、在庫あり→10）

**測試結果**（5 項商品）：
| 項目 | 結果 |
|------|------|
| 圖片下載 | ✅ 6 張圖片成功上傳到 R2 |
| 變體建立 | ✅ 33 個變體正確建立 |
| 顏色翻譯 | ✅ ブラック→黑色、ベージュ→奶茶、チャコールグレー→鐵灰、オリーブ→軍綠、ネイビー→深藍 |
| 尺寸保留 | ✅ S, M, L |
| 標題翻譯 | ✅ 部分關鍵字翻譯 |
| 價格轉換 | ✅ JPY → TWD (¥9999→NT$2500, ¥5995→NT$1500) |

### Freak Store 批量上架系統邏輯整合 (2026-01-29) ✅
**需求**：用戶要求嚴格遵照 `freak store批量上架系統` 資料夾中的成熟邏輯

**整合內容**：
來源檔案：`freak store批量上架系統/html_parser.py`

- [x] **COLOR_MAP** - 日文顏色 → SKU 縮寫代碼（31 個顏色）
  - 例如：`ブラック` → `BLK`, `ホワイト` → `WHT`, `ネイビー` → `NVY`

- [x] **COLOR_DISPLAY_MAP** - 日文顏色 → 中文顯示名稱（45+ 個顏色）
  - 例如：`ブラック` → `黑色`, `ベージュ` → `奶茶`, `ライトブルー` → `天空藍`

- [x] **DIMENSION_LABEL_MAP** - 尺寸表部位翻譯（25+ 個部位）
  - 衣服：`着丈` → `衣長`, `肩幅` → `肩寬`, `身幅` → `胸寬`
  - 褲子：`ウエスト` → `腰圍`, `股上` → `褲襠`, `股下` → `褲長`
  - 帽子：`内周` → `頭圍`, `深さ` → `帽深`
  - 包包：`持ち手高さ` → `提把高`, `マチ` → `包包深度`

- [x] **STOCK_STATUS_MAP** - 庫存狀態 → 數量
  - `在庫あり` → 10, `残りわずか` → 2, `在庫なし` → 0

- [x] **SKU 生成邏輯** - 格式: `FS-{hash4}-{colorCode}-{size}`
  - 使用 MD5 哈希值前 4 位大寫
  - 使用原始日文顏色生成 SKU（與 Freak Store 一致）
  - 範例：`FS-A1B2-BLK-M`, `FS-C3D4-NVY-L`

**修改檔案**：
- `src/app/api/scraper/import/route.ts`
  - 新增 `COLOR_MAP`, `COLOR_DISPLAY_MAP`, `DIMENSION_LABEL_MAP`, `STOCK_STATUS_MAP`
  - 新增 `simplifyColorName()` - 獲取顏色縮寫代碼
  - 新增 `shortHash()` - 生成 4 位 MD5 哈希
  - 新增 `generateSKU()` - Freak Store 格式 SKU 生成
  - 新增 `translateDimensionLabel()` - 尺寸表部位翻譯
  - 新增 `getStockQuantity()` - 庫存狀態轉數量
  - 更新 `processVariants()` - 使用新的翻譯和 SKU 邏輯

**下一步**：
- 使用 Chrome Extension 重新爬取 Daytona Park 商品（使用更新後的選擇器）
- 重新匯入商品並驗證翻譯/SKU/圖片

---

## 🚧 準備中

### 第三方登入 (Phase 10) ✅
- [x] LINE Login 按鈕 integration（已存在）
- [x] 自動綁定 LINE User ID（已存在）
- [x] 登入/註冊/忘記密碼頁面 UI/UX 優化

---

## 📋 待辦事項 (Backlog)

### 階段十：LINE Login ✅
- [x] 登入頁 LINE Login 按鈕
- [x] 自動綁定 LINE User ID
- [x] 忘記密碼/重設密碼頁面
- [x] 認證頁面 UI/UX 優化

### 階段十一：商品頁整合
- [x] 商品頁愛心 Icon（加入願望清單）
- [x] 缺貨時「補貨通知」按鈕
- [ ] 遺棄購物車 LINE 提醒

### 階段十二：通用爬蟲系統 (Chrome Extension 架構) 🚧
- [x] **Phase 1：Payload CMS 基礎架構** ✅
  - [x] 建立 `ScraperPlatforms` Collection（平台配置）
  - [x] 建立 `ScrapingJobs` Collection（爬取任務）
  - [x] 建立 `ScrapedProducts` Collection（暫存原始資料）
  - [x] 建立 `ScraperSettings` Global（全域設定）
  - [x] 建立 `/admin/scraper/` 頁面骨架 + 側邊欄導覽
- [x] **Phase 2：Chrome Extension 開發** ✅
  - [x] Manifest V3 + Service Worker
  - [x] Content Script（頁面爬取邏輯）
  - [x] Background Script（API 通訊）
  - [x] Popup 快速操作面板
  - [x] API Client（REST + SSE 通訊）
- [x] **Phase 3：API 通訊層** ✅
  - [x] REST API 端點（任務 CRUD、商品上傳）
  - [x] SSE 端點（即時指令推送）
  - [x] Extension ↔ Backend 雙向通訊
  - [x] 爬取指令下發 / 結果回傳協議
- [x] **Phase 4：Admin UI（使用 UI/UX Pro Max）** ✅
  - [x] 控制台升級（即時資料、任務控制、API Key 顯示）
  - [x] 爬取任務控制台（暫停/繼續/停止操作）
  - [x] 進度條即時更新
  - [x] Settings API 端點
- [x] **Phase 5：多平台選擇器配置** ✅
  - [x] Daytona Park 選擇器
  - [x] BEAMS 選擇器
  - [x] ZOZOTOWN 選擇器
  - [x] United Arrows 選擇器
  - [x] FREAK'S STORE 選擇器
  - [x] 翻譯對照表（類別/顏色/尺寸）
  - [x] Seed API 端點
- [x] **Phase 6：商品匯入邏輯** ✅
  - [x] Import API 端點
  - [x] 價格計算（多層級定價公式）
  - [x] 圖片下載與上傳
  - [x] 變體處理（顏色/尺寸）
  - [x] Slug 生成
- [x] **Phase 7：端到端測試** ✅
  - [x] Extension 安裝測試
  - [x] 完整爬取流程測試
  - [x] 匯入流程測試（10 筆 Daytona Park 商品）

### 階段十三：部署與最後驗證
- [ ] Zeabur 部署 (Next.js + MongoDB)
- [ ] 環境變數與網域設定
- [ ] 最終全面整合測試

---

## 📊 進度統計
| 階段 | 狀態 | 完成度 |
|------|------|--------|
| 階段一：基礎設施 | 已完成 | 100% |
| 階段二：後端核心 | 已完成 | 100% |
| 階段三：商業邏輯 | 已完成 | 100% |
| 階段四：Scrapbook 前端 | 已完成 | 100% |
| 階段五：Admin 客製化 | 已完成 | 100% |
| 階段六：分析報表 | 已完成 | 100% |
| 階段七：LINE Bot 整合 | 已完成 | 100% |
| 階段八：會員中心 | 已完成 | 100% |
| **階段九：購物車系統** | **已完成** | **100%** |
| **階段十：LINE Login** | **已完成** | **100%** |
| **階段十一：商品頁整合** | **進行中** | **85%** |
| **階段十二：通用爬蟲系統** | **Phase 7 完成** | **100%** |

---

## 📝 最近更新日誌

### 2026-01-30 (MongoDB 配額問題修復 - 批量匯入)
**問題**：批量匯入商品時報錯 "you are over your space quota, using 521 MB of 512 MB"

**根因分析**（花費大量時間調試後發現）：
- **不是** R2 儲存空間問題（R2 使用 4.5 GB 正常）
- **是** MongoDB Atlas 免費版（M0）的 512 MB 儲存限制
- `scraped-products` 集合佔用了 659 MB
- 原因：每個爬取商品包含完整的 Base64 圖片數據（平均 6.74 MB/商品）

**建立的診斷腳本**：
- `scripts/check-mongodb-size.ts` - 分析 MongoDB 各集合大小
- `scripts/cleanup-scraped-products.ts` - 清理已匯入的 scraped-products
- `scripts/find-orphan-r2-files.ts` - 找出 R2 孤立檔案
- `scripts/cleanup-unused-media.ts` - 清理未使用的 Media 記錄
- `scripts/delete-largest-files.ts` - 刪除最大的 R2 檔案

**清理結果**：
| 項目 | 清理前 | 清理後 |
|------|--------|--------|
| MongoDB 資料大小 | 393 MB | 35.81 MB |
| scraped-products | 89 個 | 2 個（保留測試用） |
| 刪除商品數 | - | 87 個 |

**驗證結果**：
```
嘗試上傳第一張圖片到 Media...
✅ 圖片上傳成功!
   Media ID: 697c42fb78b2a50e534d69df
   URL: https://pub-0253cb3d5f6644fc95fad66f6d98c09e.r2.dev/media/test-1769751291491.jpeg
```

**重要發現**：
- MongoDB Atlas M0 免費版限制 512 MB
- scraped-products 儲存 Base64 圖片會快速超限
- 建議：匯入後立即清除 scraped-products 的 Base64 數據，或升級 MongoDB

**下一步**：
- [x] 測試批量匯入功能（透過 Admin UI）
- [x] 自動清理機制：匯入成功後刪除 Base64 數據
- [x] 分批匯入功能：每批 50 個，避免超過 MongoDB 512MB 限制

### 2026-01-30 (自動分批匯入 + Base64 清理功能)
**需求**：實作自動分批匯入和 Base64 清理，讓免費 MongoDB 也能處理大量商品

**實作內容**：

1. **Import API 自動清理** (`src/app/api/scraper/import/route.ts`)
   - 匯入成功後自動清除 scraped-product 的 Base64 數據
   - 保留 imageUrls 結構（url, colorName）但移除 base64 欄位
   - 記錄釋放的空間大小到 console

2. **BatchImportButton 分批處理** (`src/components/Admin/Scraper/BatchImportButton.tsx`)
   - 每批處理 50 個商品（BATCH_SIZE = 50）
   - 即時進度條顯示
   - 顯示當前批次、成功/失敗數量
   - 批次失敗時詢問是否繼續
   - 批次之間延遲 1 秒讓 MongoDB 釋放空間

3. **手動清理腳本** (`scripts/cleanup-base64-data.ts`)
   - 清理所有 scraped-products 的 Base64 數據
   - 支援 `--dry-run` 預覽模式
   - 顯示清理前後的資料庫大小

**使用方式**：
```bash
# 預覽可清理的 Base64 數據
npx tsx scripts/cleanup-base64-data.ts --dry-run

# 執行清理
npx tsx scripts/cleanup-base64-data.ts
```

**架構說明**：
```
大量匯入流程：
┌─────────────────────────────────────────────────────────┐
│ 1. 選取 100 個商品                                       │
│ 2. 系統自動分 2 批（每批 50 個）                          │
│ 3. 第 1 批：匯入 50 個 → 自動清除 Base64 → 釋放 ~350MB   │
│ 4. 等待 1 秒                                             │
│ 5. 第 2 批：匯入 50 個 → 自動清除 Base64 → 釋放 ~350MB   │
│ 6. 完成！MongoDB 保持在 512MB 以下                       │
└─────────────────────────────────────────────────────────┘
```

---

### 2026-01-30 (R2 儲存空間清理 + 上傳功能修復)
**問題**：批量匯入商品時報錯 "you are over your space quota, using 521 MB of 512 MB"

**根因分析**：
- r2.dev 公開開發 URL 有 512 MB 頻寬限制（非 R2 實際儲存限制）
- R2 實際使用量為 6.26 GB，遠超過 r2.dev 的配額
- 需要自定義域名或清理儲存空間

**解決方案**：
- 短期：清理 R2 儲存空間到 4.5 GB
- 長期：將域名遷移到 Cloudflare 後，設定 `images.takemejapan.com` 自定義域名

**建立的腳本**：
- `scripts/cleanup-r2-to-target.ts` - 刪除最舊檔案直到達標
- `scripts/cleanup-r2-simple.ts` - 孤立檔案檢測（簡化版）
- `scripts/test-r2-direct.ts` - 直接測試 R2 上傳

**清理結果**：
| 項目 | 清理前 | 清理後 |
|------|--------|--------|
| 儲存空間 | 5.74 GB | 4.50 GB |
| 刪除檔案數 | - | 6,974 個 |
| 釋放空間 | - | 1.24 GB |

**驗證結果**：
```
✅ R2 上傳成功!
   Key: media/test-upload-1769749266597.png
   ETag: "02c4278e5dc76862c17c04b3bd51946d"
   ✅ 測試檔案已刪除
```

**下一步**：
- [ ] 測試批量匯入功能（透過 Admin UI）
- [ ] 未來：將 takemejapan.com 域名遷移至 Cloudflare
- [ ] 未來：設定 `images.takemejapan.com` 自定義域名以解除 r2.dev 限制

---

### 2026-01-30 (批量匯入 UI 功能)
**需求**：用戶在「商品預覽」頁面勾選商品後，找不到「匯入到商品」按鈕

**根因分析**：
- 匯入 API 存在 (`/api/scraper/import`)，但沒有對應的前端 UI
- ScrapedProducts collection 使用 Payload 預設的列表視圖

**解決方案**：建立 BatchImportButton 組件
1. **新增組件** `src/components/Admin/Scraper/BatchImportButton.tsx`
   - 使用 Payload 的 `useSelection` hook 取得選取的商品 ID
   - 支援「全選」和「個別選取」兩種模式
   - 確認對話框說明匯入操作內容
   - 呼叫 `/api/scraper/import` API 執行批量匯入
   - 顯示匯入結果（成功/失敗數量）
   - 匯入成功後自動重新載入頁面

2. **更新 Collection 配置** `src/collections/Scraper/ScrapedProducts.ts`
   - 新增 `admin.components.beforeListTable` 配置
   - 綁定 BatchImportButton 組件

3. **更新 Import Map**
   - 執行 `pnpm generate:importmap`
   - 組件已正確註冊

**使用方式**：
1. 前往 Admin → 爬蟲系統 → 商品預覽
2. 勾選要匯入的商品
3. 頁面頂部出現黃色區塊，顯示「已選取 N 個商品」
4. 點擊「批量匯入到商品」按鈕
5. 確認後等待匯入完成

**匯入功能**：
- 自動翻譯顏色名稱（日文 → 中文）
- 自動生成 SKU（Freak Store 格式）
- 處理並上傳 Base64 圖片
- 建立變體類型和選項
- 過濾已匯入的商品（不重複匯入）

### 2026-01-30 (Chrome Extension 深層爬取功能實作)
**需求**：批量爬取時圖片無法下載，因為列表頁爬取只擷取卡片基本資訊，沒有進入商品頁獲取完整資料

**根因分析**：
| 爬取模式 | 資料來源 | Base64 下載 | 結果 |
|---------|---------|------------|------|
| 單一商品 URL | 商品詳情頁 | ✅ 是 | ✅ 完整資料 |
| 批量列表爬取 | 列表卡片 | ❌ 否 | ❌ 只有標題/價格 |

**解決方案**：實作「深層爬取模式」
1. 在列表頁提取所有商品 URL
2. 逐一在新分頁中開啟商品頁
3. 執行完整的 `extractProductDetails`（含圖片 Base64 下載）
4. 關閉分頁後繼續下一個商品
5. 翻頁到下一頁列表，重複流程

**修改檔案**：

1. **`chrome-extension/content/scraper.js`**
   - 新增 `scrapeListingPageDeep()` - 深層爬取主函數
   - 新增 `extractProductUrls()` - 從列表提取商品 URL
   - 新增 `scrapeProductInNewTab()` - 透過 Background 爬取單一商品
   - 新增 `scrapeSingleProduct` 指令處理器
   - 新增 `extractProductDetailsForDeepScrape()` - 深層爬取用的詳情提取
   - 新增 `getDefaultProductSelectors()` - 預設 Daytona Park 選擇器

2. **`chrome-extension/background/service-worker.js`**
   - 新增 `SCRAPE_PRODUCT_PAGE` 訊息處理器
   - 新增 `scrapeProductInNewTab()` 函數
     - 建立新分頁
     - 等待頁面載入
     - 注入 Content Script
     - 發送 SCRAPE_SINGLE_PRODUCT 指令
     - 等待結果並關閉分頁

**使用方式**：
- 深層爬取（預設）：`config.type = 'deep'` → 完整資料含圖片
- 淺層爬取（快速）：`config.type = 'shallow'` → 只有基本資訊

**下一步**：
- 重新載入 Chrome Extension
- 測試深層爬取功能
- 驗證圖片是否正確下載並上傳

### 2026-01-30 (深層爬取 Timeout 修復)
**問題**：深層爬取測試時，任務卡住不動，商品頁分頁沒有被關閉

**根因分析**：
- `sendToContentScript` 沒有超時機制，如果 Content Script 不回應會永遠等待
- 每個商品頁有 14+ 張圖片，Base64 下載很慢
- 沒有足夠的調試日誌來定位問題

**修復內容**：

1. **`chrome-extension/background/service-worker.js`**
   - 新增 `withTimeout()` 工具函數 - Promise 超時包裝器
   - `scrapeProductInNewTab()` 各步驟加上超時：
     - Tab 載入：35 秒超時
     - Content Script 注入：10 秒超時
     - 爬取指令執行：60 秒超時
   - 新增 `maxImages: 10` 參數限制圖片數量
   - 增強調試日誌

2. **`chrome-extension/content/scraper.js`**
   - `scrapeSingleProduct()` 接收 `maxImages` 參數
   - `extractProductDetailsForDeepScrape()` 使用 `maxImages` 限制下載數量
   - 增強調試日誌（START/END 標記）

**超時設定**：
| 步驟 | 超時時間 |
|------|---------|
| Tab 載入 | 35 秒 |
| Content Script 注入 | 10 秒 |
| 單一商品爬取 | 60 秒 |

**預設圖片限制**：10 張（深層爬取模式）

### 2026-01-30 (深層爬取 Bug 修復 + 詳細日誌)
**問題診斷**：用戶反映深層爬取完全沒有運行，而不是運行慢

**發現的 Bug**：
1. **回應結構不匹配** - Content Script 期望 `response.product`，但 Background 實際返回 `{ success: true, data: { product } }`
   - 修復位置：`content/scraper.js` 的 `scrapeProductInNewTab()`
   - 修復方式：改為檢查 `response?.data?.product || response?.product`

**其他調整**：
- `maxImages`: 10 → 50（保留完整圖片）
- `SCRAPE_TIMEOUT`: 60s → 120s（配合更多圖片）
- `concurrency`: 5 → 8（並行下載加速）

**新增詳細日誌**：
- Content Script: `[STEP 1]`, `[STEP 2]` 標記、emoji 指示器
- Background Script: 完整流程追蹤（建立 Tab → 載入 → 注入 → 爬取 → 關閉）
- 每個步驟都有 ✅/❌ 結果標記

**日誌查看位置**：
1. **列表頁 Console**（Daytona Park 搜尋頁）
   - `[Scraper] 🚀 DEEP SCRAPING MODE STARTED`
   - `[Scraper] [STEP 1] Extracting product URLs...`
   - `[Scraper] [STEP 2] Calling scrapeProductInNewTab...`

2. **Service Worker Console**（chrome://extensions → Extension 詳細資訊 → 檢查 Service Worker）
   - `[ServiceWorker] 🚀 scrapeProductInNewTab START`
   - `[ServiceWorker] [STEP 1-5]` 流程追蹤

**下一步**：重新載入 Extension 並測試

---

### 2026-01-30 (Chrome Extension 系統維護)
**完成項目**：

1. **修復批量上傳 HTTP 500 錯誤** ✅
   - **問題**：Extension 上傳 100 個商品到 `/api/scraper/products` 時返回 HTTP 500
   - **根因**：`ScrapedProducts` Collection 的 `externalId` 欄位是 required
   - **但**：`formatProductData()` 設定 `externalId: product.externalId || null`
   - **修復**：新增 `extractIdFromUrl()` 函數，從 URL 提取商品 ID
     - 例如：`https://www.daytona-park.com/item/3130291600006` → `3130291600006`
   - **同時**：新增驗證，跳過沒有 `externalUrl` 的商品
   - **改進**：加強錯誤日誌，顯示 Payload validation errors

2. **重置卡住的爬取任務** ✅
   - 建立 `scripts/reset-stuck-jobs.ts` 腳本
   - 重置 3 個卡在 "running" 狀態的任務為 "failed"
   - 任務 ID：`697b8a2a`, `697b89a9`, `697b7cc2`
   - 原因：Extension 重新載入導致 activeJobs Map 被清空

2. **更新平台 sizeChart 選擇器** ✅
   - 建立 `scripts/update-platform-selectors.ts` 腳本
   - 更新 Daytona Park 平台的 `productSelectors.sizeChart` 選擇器
   - 新值：`table.block-goods-product-size-table`
   - 這將使未來的爬取任務能夠正確擷取尺寸表

**新增腳本**：
- `scripts/reset-stuck-jobs.ts` - 重置卡住的爬取任務
- `scripts/update-platform-selectors.ts` - 更新平台選擇器配置

---

### 2026-01-29 (Chrome Extension 爬取流程調試)
**問題描述**：Extension 已連線成功，但任務顯示「執行中」卻沒有實際爬取進度

**已完成調試**：
1. ✅ API Key 配置修正 - 硬編碼正確的 API Key 到 `api-client.js`
2. ✅ Extension 連線成功 - Service Worker 顯示 `[ApiClient] Connected`
3. ✅ SSE 心跳正常 - 顯示 `[ApiClient] SSE heartbeat`
4. ✅ Content Script 載入 - 頁面 Console 顯示 `[Scraper] Content script loaded`

**新增調試日誌**：
- `service-worker.js` - COMMAND 處理器新增詳細日誌
  - 原始訊息、解析後的 action/jobId/config
  - handleStartJob 各步驟追蹤（驗證、Tab 建立、載入等待、注入、發送）
  - 重複任務防護機制
- `content/scraper.js` - 訊息接收新增詳細日誌
  - 接收到的原始訊息
  - 訊息類型和 payload
  - 指令處理追蹤

**下一步**：
- [ ] 重新載入 Extension 並創建新任務
- [ ] 觀察 Service Worker 日誌，確認 START_JOB 指令是否被處理
- [ ] 觀察 Content Script 日誌，確認是否收到 START_SCRAPE 訊息
- [ ] 根據日誌追蹤問題點

**修改檔案**：
- `chrome-extension/background/service-worker.js` - 新增調試日誌
- `chrome-extension/content/scraper.js` - 新增訊息接收日誌

### 2026-01-29 (前端幣別配置修復 + 價格顯示驗證)
**問題描述**：使用 Chrome Extension 匯入的商品在前端顯示價格為空（不顯示）

**根因分析**：
1. 商品資料正確設定了 `priceInTWD` 和 `priceInTWDEnabled` 欄位
2. 但 `EcommerceProvider` 未設定 `currenciesConfig`，導致 `useCurrency()` hook 預設使用 USD
3. 前端 `ProductDescription.tsx` 使用 `priceIn${currency.code}` 動態查詢價格欄位
4. 因為預設 USD，系統查詢 `priceInUSD` 但商品只有 `priceInTWD` 資料

**解決方案**：
- **修改檔案**：`src/providers/index.tsx`
- 在 `EcommerceProvider` 新增 `currenciesConfig` prop，與 server 端 plugin config 保持一致：
  ```typescript
  currenciesConfig={{
    defaultCurrency: 'TWD',
    supportedCurrencies: [TWD, JPY, USD],
  }}
  ```
- 新增自定義幣別物件（TWD, JPY）與 server 端同步

**驗證結果**：
| 商品 | 價格 | 狀態 |
|------|------|------|
| カーゴパンツ (Cargo Pants) | NT$2480 | ✅ 正確顯示 |
| イージーパンツ (Easy Pants) | NT$2190 | ✅ 正確顯示 |

**其他驗證項目**：
- ✅ 變體矩陣正確顯示所有顏色和尺寸
- ✅ 每個尺寸都有獨立的收藏按鈕
- ✅ 庫存狀態正確顯示（有庫存/缺貨）
- ✅ 商品描述和尺寸表正確渲染

---

### 2026-01-29 (Chrome Extension Base64 圖片傳輸功能)
**需求**：解決 `images.daytona-park.com` 伺服器地理限制導致後端無法下載圖片的問題

**解決方案**：在 Chrome Extension 中下載圖片並轉換為 Base64，隨產品資料一起傳送到後端

**修改檔案**：
1. **`chrome-extension/content/scraper.js`**
   - 新增 `downloadImageAsBase64()` 函數 - 在瀏覽器環境下載圖片並轉為 Base64
   - 修改 `extractImages()` 為非同步函數，支援 Base64 下載
   - 並行下載（每批 5 張），含進度回報
   - 支援重試機制（最多 3 次）

2. **`src/app/api/scraper/products/route.ts`**
   - `formatProductData()` 保留 `base64` 欄位

3. **`src/app/api/scraper/import/route.ts`**
   - 新增 `parseBase64Image()` 函數 - 解析 Base64 字串
   - 修改 `processImages()` 優先使用 Base64 資料，URL 下載為 fallback
   - 支援最多 40 張圖片（從 15 張提升）

4. **`src/collections/Scraper/ScrapedProducts.ts`**
   - `imageUrls` 陣列新增 `base64` 欄位

**架構說明**：
```
Chrome Extension (瀏覽器環境)
    ↓ 下載圖片為 Base64
    ↓ 傳送到後端
Payload API (/api/scraper/products)
    ↓ 儲存 Base64 到 ScrapedProducts
Import API (/api/scraper/import)
    ↓ 解析 Base64 → Buffer
    ↓ 上傳到 R2 Media
```

### 2026-01-29 (變體匯入深度調試)
**問題描述**：驗證匯入商品時發現嚴重問題
- 商品 `enableVariants: true`, `variantTypes: 2` 但 `variants: 0`
- 變體完全沒有被建立
- Gallery 圖片沒有關聯到變體選項

**調試過程**：
1. 檢查 ecommerce plugin 的 variants 集合結構
2. 發現 `validateOptions` hook 要求每個變體的 options 數量必須等於商品的 variantTypes 數量
3. 發現 `variantsCollectionOverride` 中添加的 SKU 欄位沒有生效（未出現在 payload-types.ts）
4. 建立多個測試腳本驗證匯入流程

**修復內容**：
- [x] **新增匯入日誌** (`src/app/api/scraper/import/route.ts`)
  - `processVariants()` 添加詳細日誌：variantTypeMap 結構、選項數量
  - 變體建立添加 try-catch 和錯誤日誌
  - 每個變體建立前記錄 title, options, inventory
- [x] **建立測試腳本**
  - `scripts/test-variant-import.ts` - 測試單一變體建立
  - `scripts/test-reimport.ts` - 測試完整變體建立流程
  - `scripts/test-full-import.ts` - 模擬完整 API 匯入流程
  - `scripts/check-variants.ts` - 檢查商品變體數據

**驗證結果**（測試 15 個變體的商品）：
| 項目 | 結果 |
|------|------|
| 變體建立 | ✅ 15/15 成功 |
| options 數量 | ✅ 每個變體 2 個選項（顏色+尺寸）|
| inventory | ✅ 每個變體 10（正確） |
| 標題生成 | ✅ 商品名 — 顏色 — 尺寸 |

**待處理問題**：
- [ ] **SKU 欄位不生效**：`variantsCollectionOverride` 添加的欄位未反映在 schema
- [ ] **Gallery variantOption**：圖片未關聯到顏色變體選項

**結論**：
- 變體建立機制本身正常運作
- 原始匯入失敗可能是因為執行時的代碼版本或中斷
- 現有日誌可幫助追蹤未來的匯入問題

### 2026-01-29 (匯入系統 4 項 Bug 修復)
根據用戶前端驗收回報的問題，完成以下修復：

**1. 尺寸排序修正 (S > M > L)**
- **問題**：前端商品頁顯示尺寸順序為 L > M > S，應為 S > M > L
- **修改檔案**：`src/components/product/VariantMatrix.tsx`
- **解決方案**：
  - 新增 `SIZE_ORDER` 排序權重表（XXS=1, XS=2, S=3, M=4, L=5, XL=6...）
  - 新增 `getSizeOrder()` 函數處理數字尺寸和未知尺寸
  - 在 `ColorGroupCard` 組件中使用 `useMemo` 對變體排序

**2. 庫存狀態映射補充**
- **問題**：日文庫存狀態 `予約する` 未映射，導致庫存顯示錯誤
- **修改檔案**：`src/app/api/scraper/import/route.ts`
- **解決方案**：在 `STOCK_STATUS_MAP` 新增 `'予約する': 7`

**3. 價格顯示修復**
- **問題**：前端商品頁價格不顯示
- **修改檔案**：`src/components/product/ProductDescription.tsx`
- **根因分析**：
  - 原程式碼在陣列可能為空時直接存取 `[0]` 導致錯誤
  - 價格欄位檢查邏輯不夠嚴謹
- **解決方案**：
  - 重構變體價格提取邏輯，加入完整的型別守衛
  - 先過濾出有效且有價格的變體再排序
  - 陣列長度為 0 時不嘗試存取元素

**4. 圖片選擇器更新**
- **問題**：圖片擷取地址與實際網頁結構不符
- **修改檔案**：`src/lib/scraper/platform-seeds.ts`
- **解決方案**：
  - 更新 Daytona Park 圖片選擇器為多選擇器
  - 新選擇器：`div.image-list img, .block-goods-color-variation-img img, .swiper-slide img`
  - 新增圖片篩選規則註解（只接受 `https://images.` URL，排除 `video-thumb`）

### 2026-01-29 (商品匯入腳本完成)
- ✅ **建立測試腳本**
  - `scripts/create-test-scraping-job.ts` - 建立爬取任務和爬取商品
  - `scripts/import-scraped-products.ts` - 匯入爬取商品到 Products
  - `scripts/cleanup-test-data.ts` - 清理測試數據
  - `scripts/delete-all-scraped-products.ts` - 刪除所有爬取商品
- ✅ **匯入腳本功能完成**
  - 圖片下載與上傳至 R2
  - 正確建立 variantTypes、variantOptions、variants
  - 顏色翻譯（日文 → 中文）
  - 部分標題關鍵字翻譯
  - 商品描述生成（固定模板 + 動態尺寸表）
  - SKU 生成（Freak Store 格式）
  - 庫存狀態轉換
- ✅ **測試結果**
  - 5 項商品成功匯入
  - 33 個變體正確建立
  - 6 張圖片上傳至 R2
- ✅ **瀏覽器驗證完成** (2026-01-29)
  - Admin 後台：確認商品列表、變體類型（顏色/尺寸）、變體選項
  - 前台商品頁：確認 VariantMatrix 顯示 5 種顏色（奶茶、軍綠、深藍、黑色、鐵灰）
  - 動態尺寸表：確認顯示 S/M/L 各尺寸的腰圍、褲腳、大腿圍、臀圍
  - 補貨通知：確認缺貨變體顯示「到貨通知」按鈕
  - 收藏功能：確認每個尺寸都有獨立的愛心收藏按鈕
- **執行方式**
  ```bash
  # 1. 建立測試數據
  npx tsx scripts/create-test-scraping-job.ts

  # 2. 匯入商品
  npx tsx scripts/import-scraped-products.ts

  # 3. 清理（如需重新測試）
  npx tsx scripts/cleanup-test-data.ts
  npx tsx scripts/delete-all-scraped-products.ts
  ```

### 2026-01-28 (通用爬蟲系統 Phase 7 端到端測試完成)
- ✅ **Phase 7: 端到端測試**
  - Daytona Park 新品頁前 10 項商品測試
  - 測試 URL: `https://www.daytona-park.com/itemlist?stock=1&sort=new`
- ✅ **Bug 修復清單**
  - `ScraperSettings.ts`: 新增缺失的 `enabled` 和 `apiKey` 欄位
  - `/api/scraper/route.ts`: 修正 `enabled` → `isActive` 欄位名稱
  - `/api/scraper/seed/route.ts`: 修正 `formatPlatformData()` 匹配 Collection 結構
  - `/api/scraper/import/route.ts`: 多項修正
    - `variant-types` → `variantTypes` Collection slug
    - `variant-options` → `variantOptions` Collection slug
    - `pricing.originalPrice` → `originalPrice` 欄位路徑
    - `images` → `imageUrls` 欄位路徑
    - `price` → `priceInTWD` + `priceInTWDEnabled` 價格欄位
    - 新增 `vendor` 必填欄位處理
    - 新增 `variantTypes.name` 必填欄位
    - 新增 `variantOptions.value` 必填欄位（修復 "Value" 錯誤）
- ✅ **測試結果**
  - Platform Seeding: 5 個平台成功建立
  - Job Creation: 爬取任務成功建立
  - Product Upload: 10 筆爬取商品成功上傳
  - Product Import: 10 筆商品成功匯入 Products Collection
    - 商品：FIRE BIRD TRACK PANTS、SAMBA OG、TAEKWONDO MEI 等
    - 狀態：Draft、Vendor: Official Store、enableVariants: true

### 2026-01-28 (通用爬蟲系統 Phase 4-6 實作完成)
- ✅ **Phase 4: Admin UI 升級**
  - Dashboard 從 API 取得真實資料
  - 任務控制功能（暫停/繼續/停止）
  - API Key 顯示/複製功能
  - 即時進度更新（10 秒輪詢）
  - `src/app/api/scraper/settings/route.ts` (新增)
- ✅ **Phase 5: 多平台選擇器配置**
  - 建立 5 個平台的 CSS 選擇器配置
    - Daytona Park
    - BEAMS
    - ZOZOTOWN
    - United Arrows
    - FREAK'S STORE
  - 翻譯對照表（類別/顏色/尺寸 共 80+ 項）
  - `src/lib/scraper/platform-seeds.ts` (新增)
  - `src/app/api/scraper/seed/route.ts` (新增)
- ✅ **Phase 6: 商品匯入邏輯**
  - ScrapedProducts → Products 匯入 API
  - 多層級價格計算公式
  - 圖片下載與 Media 上傳
  - 變體類型/選項自動建立
  - `src/app/api/scraper/import/route.ts` (新增)

### 2026-01-28 (通用爬蟲系統 Phase 2+3 實作完成)
- ✅ **Chrome Extension 開發完成**
  - `chrome-extension/manifest.json` - Manifest V3 配置
  - `chrome-extension/lib/constants.js` - 常數定義（訊息類型、狀態）
  - `chrome-extension/lib/api-client.js` - REST + SSE 客戶端
  - `chrome-extension/lib/message-handler.js` - 組件間訊息傳遞
  - `chrome-extension/lib/websocket-client.js` - WebSocket 客戶端（備用）
  - `chrome-extension/background/service-worker.js` - 背景任務管理
  - `chrome-extension/content/scraper.js` - DOM 爬取邏輯
  - `chrome-extension/popup/popup.html` - 快速操作面板
  - `chrome-extension/popup/popup.css` - 面板樣式
  - `chrome-extension/popup/popup.js` - 面板互動邏輯
- ✅ **API 通訊層完成**
  - `src/app/api/scraper/route.ts` - 系統狀態 + 建立任務
  - `src/app/api/scraper/jobs/route.ts` - 任務輪詢 + 狀態更新
  - `src/app/api/scraper/products/route.ts` - 商品數據上傳
  - `src/app/api/scraper/events/route.ts` - SSE 即時指令
- ✅ **通訊機制**
  - REST API：任務 CRUD、商品批次上傳
  - SSE：後端即時推送指令到 Extension
  - Token 認證：Extension 使用 API Key 驗證

### 2026-01-28 (通用爬蟲系統 Phase 1 實作完成)
- ✅ **Payload CMS 基礎架構完成**
  - 新增 `ScraperPlatforms` Collection（平台配置、選擇器、定價規則）
  - 新增 `ScrapingJobs` Collection（任務狀態、進度追蹤、日誌）
  - 新增 `ScrapedProducts` Collection（暫存原始資料、匯入狀態）
  - 新增 `ScraperSettings` Global（定價層級、翻譯對照表、匯入設定）
- ✅ **Admin UI 骨架完成**
  - 新增 `/admin/scraper` 控制台頁面
  - Extension 連線狀態列
  - 快速導覽卡片（平台管理/爬取任務/商品預覽）
  - 快速建立任務表單
  - 執行中/歷史任務列表
- ✅ **側邊欄導覽更新**
  - 新增「爬蟲系統」群組
  - 控制台、平台管理、爬取任務、商品預覽、爬蟲設定
- **修改檔案**:
  - `src/collections/Scraper/ScraperPlatforms.ts` (新增)
  - `src/collections/Scraper/ScrapingJobs.ts` (新增)
  - `src/collections/Scraper/ScrapedProducts.ts` (新增)
  - `src/globals/ScraperSettings.ts` (新增)
  - `src/components/Admin/Scraper/Dashboard/View.tsx` (新增)
  - `src/components/Admin/Scraper/Dashboard/index.tsx` (新增)
  - `src/payload.config.ts` (更新)
  - `src/components/Admin/NavClient.tsx` (更新)

### 2026-01-28 (通用爬蟲系統規劃 v3 - Chrome Extension 架構)
- ✅ **系統架構升級為 Chrome Extension 方案**
  - 文件：`memory-bank/freakstore-auto-scraper-plan.md` (v3)
  - 優勢：一次安裝、後台控制、無需手動貼腳本
- ✅ **多平台支援架構設計**
  - 支援平台：Daytona Park、BEAMS、ZOZOTOWN 等日本服飾網站
  - 平台配置表：CSS 選擇器、URL 規則、定價公式
  - 新增 `ScraperPlatforms` Collection 管理多平台
- ✅ **Chrome Extension 架構設計**
  - Manifest V3 + Service Worker
  - Content Script 執行頁面爬取
  - WebSocket 即時通訊（Admin UI ↔ Backend ↔ Extension）
- ✅ **Payload CMS 資料結構設計**
  - `ScraperPlatforms` - 平台配置（選擇器、定價、翻譯）
  - `ScrapingJobs` - 爬取任務（狀態追蹤、進度）
  - `ScrapedProducts` - 暫存原始資料（待審核）
  - `ScraperSettings` - 全域設定
- ✅ **Admin UI 設計（UI/UX Pro Max）**
  - 設計系統：Data-Dense Dashboard
  - 色彩：主色 Blue #1E40AF、CTA Amber #F59E0B
  - 字體：Inter (內文) + JetBrains Mono (數據)
  - 頁面：平台管理、爬取控制台、商品預覽、設定
- ✅ **雙模式爬取設計**
  - 深層模式：完整商品資料、圖片、變體（用於上架）
  - 淺層模式：價格、庫存狀態（用於同步）

### 2026-01-28 (Freak Store 自動爬蟲系統規劃 v2)
- ✅ **系統規劃文件更新至 v2**
  - 文件：`memory-bank/freakstore-auto-scraper-plan.md`
- ✅ **新增雙模式上架功能設計**
  - 單一商品 URL 模式：直接輸入商品頁 URL
  - 批量列表頁模式：輸入新品/折扣/分類頁 URL
  - URLDetector 自動偵測 URL 類型
- ✅ **多層級定價公式設計**
  - Tier 1 (< ¥11,000): `price × 0.21 × 1.07 + 110 + 380`
  - Tier 2 (≥ ¥11,000): `price × 0.21 × 1.05 + 150 + 380`
  - 所有參數可在 UI 中自定義：匯率、稅率乘數、手續費、國際運費、進位規則
- ✅ **翻譯命名模組設計**
  - 日文→中文自動翻譯
  - 符合台灣電商服飾命名慣例
  - 建立翻譯對照表：類別、特徵、顏色、尺寸測量項目
  - 無法翻譯時標記「需審核」
- ✅ **UI/UX Pro Max 設計系統**
  - 風格：Data-Dense Dashboard
  - 色彩：成功綠 #059669 / CTA橘 #F97316 / 警告黃 #F59E0B
  - 字體：Fira Code (數據) + Fira Sans (內文)
- ✅ **資料庫結構擴充設計**
  - Products Collection 新增欄位：sizeChart, colorImageMapping, originalPriceJPY, japaneseTitle, translationStatus
  - 新增 FreakStoreConfig Global（定價/翻譯/匯入設定）
  - 新增 FreakStoreSyncLogs Collection（匯入記錄）
- ✅ **Admin UI 設計**
  - 批量模式 UI：URL 輸入、定價面板、翻譯面板、商品預覽表格、匯入進度
  - 單一商品模式 UI：商品詳情預覽、變體表格、尺寸表
- ✅ **目標頁面 URL 確認**
  - 新品頁：`https://www.daytona-park.com/itemlist?stock=1&sort=new`

### 2026-01-28 (Rich Text 編輯器擴展功能)
- ✅ **安裝第三方 Lexical 編輯器擴展套件**
  - `@nhayhoc/payloadcms-lexical-ext@0.5.5` - 文字顏色、高亮底色
  - `payload-lexical-typography@0.5.2` - 字體大小調整
- ✅ **Products Collection 編輯器配置更新**
  - 新增 `TextColorFeature` - RGB 顏色選擇器，支援任意顏色
    - 9 個預設顏色按鈕（黑、深灰、紅、橘、金、綠、藍、紫、粉紅）
    - 完整 RGB 調色盤，可選擇任意顏色
  - 新增 `HighlightColorFeature` - 文字底色/高亮功能
    - 5 個預設底色（黃、綠、藍、紅、紫）
    - 完整 RGB 調色盤
  - 新增 `TextSizeFeature` - 字體大小功能
    - 8 個預設大小（12px、14px、16px、18px、20px、24px、28px、32px）
    - 支援自定義大小輸入
- ✅ **EasyStore 匯入器描述轉換重新啟用**
  - `easystore-importer.ts` 中的 `htmlToLexicalJson()` 轉換已重新啟用
  - 使用自定義 HTML-to-Lexical 轉換器，保留內嵌 CSS 樣式
  - 支援的樣式：`color`、`background-color`、`font-size`、`font-weight`、`text-decoration`
- ✅ **瀏覽器驗證通過**
  - RGB 顏色選擇器：可選擇任意顏色，顯示完整調色盤
  - 字體大小下拉：可選預設值或輸入自定義大小
  - 高亮底色功能：可設定文字背景顏色
- **修改檔案**:
  - `src/collections/Products/index.ts` - 更新 lexicalEditor features
  - `src/lib/import/easystore-importer.ts` - 重新啟用描述轉換
  - `package.json` - 新增兩個依賴套件

### 2026-01-28 (Admin Panel Hydration Error 修復)
- ✅ **修復 Zeabur 生產環境 Admin 面板空白問題**
  - **問題**: Admin 面板 (`/admin`) 顯示完全空白，React Hydration Error #418
  - **原因 1**: `BeforeLogin` 組件使用 `process.env.PAYLOAD_PUBLIC_SERVER_URL`
    - 只有 `NEXT_PUBLIC_` 前綴的環境變數才能在客戶端使用
    - 服務器渲染 `https://...` 但客戶端渲染 `undefined`
    - 導致 hydration mismatch
  - **原因 2**: `NavClient` 組件在 `useState` 初始化中使用 `localStorage`
    - 服務器端 `localStorage` 不存在
    - 導致 hydration mismatch
  - **解決方案**:
    - `BeforeLogin`: 改用 `NEXT_PUBLIC_SERVER_URL`
    - `NavClient`: 將 `localStorage` 讀取移至 `useEffect`
  - **額外修復**:
    - `next.config.js`: 新增 R2 公開 URL 至 `images.remotePatterns`
- ✅ **推送至 GitHub** (commit: 2ec490e)
- ⚠️ **待確認**: Zeabur 自動重新部署後 admin 面板應恢復正常

### 2026-01-28 (Cloudflare R2 圖片儲存配置)
- ✅ **Cloudflare R2 儲存桶設定完成**
  - 建立 R2 Bucket: `takemejapan-media`
  - 位置: Asia Pacific (APAC)
  - 儲存類別: Standard
- ✅ **公開訪問 URL 已啟用**
  - Public Development URL: `https://pub-0253cb3d5f6644fc95fad66f6d98c09e.r2.dev`
  - 注意: 生產環境建議使用自定義域名（需先將 takemejapan.com 加入 Cloudflare）
- ✅ **CORS 政策已配置**
  ```json
  {
    "AllowedOrigins": ["http://localhost:3000", "https://takemejapan.com", "https://www.takemejapan.com", "https://takemejapan.zeabur.app"],
    "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
  ```
- ✅ **API Token 建立**
  - Token 名稱: `payload-cms-takemejapan`
  - 權限: Object Read and Write
  - 範圍: All buckets
- ✅ **Payload CMS 儲存插件配置**
  - 更新 `src/plugins/storage.ts`:
    - 新增 `generateFileURL` 使用公開 R2 URL
    - 新增詳細註解說明環境變數
    - 新增啟用/未啟用日誌訊息
  - 新增環境變數至 `.env`:
    - `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`
    - `S3_ENDPOINT`, `S3_REGION`, `S3_PUBLIC_URL`
- ⚠️ **待辦**:
  - 重新匯入商品圖片以使用 R2 儲存
  - 生產環境建議新增自定義域名（如 `images.takemejapan.com`）

### 2026-01-28 (登出頁面中文化)
- ✅ **登出頁面完整中文化與連結修復**
  - **`src/app/(app)/logout/LogoutPage/index.tsx`**:
    - 載入狀態：「登出中...」、「請稍候」
    - 成功狀態：「已成功登出」/「您已經登出」
    - 感謝語：「感謝您的使用，期待您再次光臨！」
    - 按鈕文字：「繼續購物」、「重新登入」
    - 連結文字：「或返回 首頁」
  - **`src/app/(app)/logout/page.tsx`**:
    - 中文 metadata：`title: '登出 | Daytona Park'`
    - 統一視覺風格（居中版面、品牌圖示、表單卡片）
  - **修復無效連結**：
    - `/search` → `/shop`（繼續購物）
    - 確保所有連結都是有效的：`/shop`、`/login`、`/`
  - **瀏覽器驗證結果**：
    - ✅ 繼續購物 → `/shop` 商店頁面
    - ✅ 重新登入 → `/login` 登入頁面
    - ✅ 首頁 → `/` 首頁

### 2026-01-28 (認證頁面 UI/UX 優化)
- ✅ **認證系統審查與優化**
  - 使用 UI/UX Pro Max 技能庫進行設計系統生成
  - 調用 Explore Agent 全面審查現有認證實作
- ✅ **修復 LoginForm 錯誤連結**
  - 問題：「忘記密碼」連結指向不存在的 `/recover-password`
  - 解決：改為正確路徑 `/forgot-password`
- ✅ **新增 /recover-password 頁面**
  - 用途：用戶點擊重設密碼郵件連結後的目標頁面
  - 功能：接收 token 參數、驗證有效性、設定新密碼
  - 新增元件：`RecoverPasswordForm` (`src/components/forms/RecoverPasswordForm/index.tsx`)
  - 新增頁面：`src/app/(app)/recover-password/page.tsx`
- ✅ **ForgotPasswordForm 翻譯與優化**
  - 全部英文文字翻譯為繁體中文
  - 新增成功畫面（郵件圖示 + 友善提示）
  - 新增 loading 狀態顯示
  - 更新 metadata 為中文
- ✅ **認證頁面 UI/UX 全面升級**
  - **統一視覺風格**：
    - 居中版面 (`min-h-[calc(100vh-200px)] flex items-center justify-center`)
    - 品牌圖示區塊（圓形背景 + SVG 圖示）
    - 表單卡片樣式 (`bg-white rounded-2xl shadow-sm border`)
    - 底部說明文字
  - **登入頁 (`/login`)**：
    - 新增「歡迎回來」標題與說明文字
    - LINE 登入按鈕移至頂部，強調快速登入
    - 優化分隔線樣式
    - 「忘記密碼」連結移至密碼欄位右側
    - 使用 scrapbook 主題色 (`#C9925E`)
  - **註冊頁 (`/create-account`)**：
    - 新增「加入會員」標題與說明文字
    - 使用 scrapbook 次要色 (`#4A6C6F`)
    - 新增電子郵件格式驗證
    - 新增密碼長度驗證（至少 8 字元）
    - 改善錯誤訊息（已註冊帳號的友善提示）
  - **忘記密碼頁 (`/forgot-password`)**：
    - 新增鑰匙圖示
    - 使用琥珀色系統色
  - **重設密碼頁 (`/recover-password`)**：
    - 新增盾牌圖示
    - Token 無效時顯示友善錯誤畫面
    - 成功後 3 秒自動跳轉登入頁
  - **表單元素優化**：
    - 輸入框高度增加至 48px (`h-12`)
    - 按鈕高度增加至 48px 符合觸控標準
    - 新增 `autoComplete` 屬性優化瀏覽器自動填入
    - Loading spinner 動畫
- **修改檔案清單**:
  - `src/components/forms/LoginForm/index.tsx`
  - `src/components/forms/CreateAccountForm/index.tsx`
  - `src/components/forms/ForgotPasswordForm/index.tsx`
  - `src/components/forms/RecoverPasswordForm/index.tsx` (新增)
  - `src/app/(app)/login/page.tsx`
  - `src/app/(app)/create-account/page.tsx`
  - `src/app/(app)/forgot-password/page.tsx`
  - `src/app/(app)/recover-password/page.tsx` (新增)

### 2026-01-28 (Gallery 展開更多 Bug 修復)
- ✅ **修復「展開更多」按鈕點擊後無圖片顯示問題**
  - **問題**: 點擊展開按鈕後，圖片顯示為全寬堆疊而非縮圖網格
  - **根因**: Tailwind CSS 4 未正確編譯 `grid-cols-5` 類別
  - **解決方案**: 在 `Gallery.tsx` 使用內聯樣式定義網格佈局
    ```tsx
    style={{ display: 'grid', gridTemplateColumns: 'repeat(5, minmax(0, 1fr))' }}
    ```
  - 現在展開後可正確顯示 5 欄縮圖網格，所有 87 張圖片皆可瀏覽

### 2026-01-28 (UI/UX Pro Max 優化)
- ✅ **商品頁 UI/UX Pro Max 全面優化**
  - 使用 UI/UX Pro Max 技能庫進行審查與優化
  - **Gallery 組件優化**:
    - 「展開更多 (N 張)」按鈕移至輪播下方，始終可見
    - 新增主圖右下角圖片計數器 (1 / 87)
    - 縮圖加入 `cursor-pointer` 提示可點擊
  - **VariantMatrix 無障礙改進**:
    - 庫存狀態：新增圖示 (✓ 有庫存 / ❌ 缺貨)，不僅依賴顏色
    - Touch Target Size：按鈕最小高度 36px，符合觸控友好標準
    - 按鈕間距：從 `gap-1` (4px) 增加至 `gap-2` (8px)
    - ARIA Labels：所有按鈕加入 `aria-label` 和 `aria-pressed`
    - Focus 狀態：加入 `focus-visible:ring-2` 可見焦點環
  - **VariantWishlistButton 優化**:
    - 尺寸從 32px 增加至 36px
    - 加入 `aria-pressed` 狀態追蹤
    - 優化 hover/active 狀態視覺回饋
  - **ProductDescription 排版改進**:
    - 標題：`text-xl` → `text-2xl lg:text-3xl font-bold`
    - 價格：更大、更醒目的顯示
    - 間距：整體 gap 從 4 增加至 5
  - **全域 CSS 無障礙支援**:
    - 新增 `prefers-reduced-motion` 減少動態效果
    - 觸控設備最小點擊區域 44px
    - `touch-action: manipulation` 減少觸控延遲
    - 全域 `:focus-visible` 焦點環樣式
  - **修改檔案**:
    - `src/components/product/Gallery.tsx`
    - `src/components/product/VariantMatrix.tsx`
    - `src/components/product/VariantWishlistButton.tsx`
    - `src/components/product/ProductDescription.tsx`
    - `src/app/(app)/globals.css`

### 2026-01-27 (晚間)
- ✅ **VariantMatrix Bug 修復 - 商品顏色顯示不完整**
  - **問題描述**：EasyStore 匯入的商品應有 5 種顏色，但只顯示 4 種
  - **根本原因 1**：變體查詢有分頁限制（limit: 10），導致 15 個變體只返回 10 個
    - 解決：單獨查詢變體，設定 `limit: 100` 和 `pagination: false`
  - **根本原因 2**：VariantMatrix 使用 `opt.type` 但正確欄位是 `opt.variantType`
    - 解決：修正欄位名稱為 `opt.variantType`
  - **修改檔案**：
    - `src/app/(app)/products/[slug]/page.tsx` - 重構 queryProductBySlug
    - `src/components/product/VariantMatrix.tsx` - 修正 variantType 欄位
  - **驗證結果**：5 種顏色（格紋深灰、格紋淺灰、深藍、棕色、黑色）全部正確顯示

### 2026-01-27
- ✅ **商品頁 UI 優化 - VariantMatrix 變體矩陣**
  - 新增 `VariantMatrix` 組件 (`src/components/product/VariantMatrix.tsx`)
    - 按顏色分組顯示所有尺寸變體
    - 每個尺寸行內顯示：庫存狀態、加入購物車/到貨通知、收藏按鈕
    - 缺貨狀態只顯示在尺寸層級，不在顏色層級
    - 每個顏色組顯示縮圖（從 gallery 關聯取得）
  - 新增 `VariantWishlistButton` 組件 (`src/components/product/VariantWishlistButton.tsx`)
    - 小型愛心按鈕，適合放在表格行內
    - 支援特定變體（顏色+尺寸）的收藏功能
  - 優化 `Gallery` 組件
    - 新增「查看更多」功能，初始顯示 10 張縮圖
    - 超過限制時顯示 "+N 更多" 計數器
    - 展開後可收合回初始狀態
  - 更新 `ProductDescription` 組件
    - 有變體商品：使用 VariantMatrix 取代舊版 VariantSelector
    - 無變體商品：保留原本的加入購物車/收藏按鈕
    - 版面更緊湊（調整 gap 和 font-size）
  - 參考設計：Daytona Park (https://www.daytona-park.com/)

- ✅ **前端驗證完成 - 圖片載入 & 版面修復**
  - 修復 `Media/Image/index.tsx` 圖片載入問題
    - 問題：使用 `NEXT_PUBLIC_SERVER_URL` 前綴導致指向生產環境 URL
    - 解決：改用相對路徑 `src = url || ''`
    - 結果：所有商品圖片正常顯示
  - 修復 `products/[slug]/page.tsx` 版面溢位問題
    - 問題：全螢幕時 Carousel 元素超出 viewport
    - 解決：在 Gallery 父容器和主商品容器加入 `overflow-hidden`
    - 結果：版面正常，無水平溢位
  - 驗證 VariantSelector 顯示正確
    - 顏色：黑色、格紋深灰、棕色、深藍（缺貨）
    - 尺寸：S（缺貨）、M、L
    - 缺貨選項可點擊，顯示紅色高亮樣式
  - 驗證 API 端點運作正常
    - `/api/users/me` - 返回用戶資料（驗證通過）
    - `/api/wishlist` - 認證修復生效
    - `/api/restock-requests` - 認證修復生效
  - ⚠️ 發現環境配置問題
    - `.env` 中 `NEXT_PUBLIC_SERVER_URL=https://takemejapan.zeabur.app`
    - 本地開發時 AuthProvider 指向生產環境，導致 CORS/cookie 問題
    - 解決方案：建立 `.env.local` 設定 `NEXT_PUBLIC_SERVER_URL=http://localhost:3000`
- ✅ **Phase 11 Bug 修復 - VariantSelector & API 認證**
  - 修復 `VariantSelector` 顯示錯誤選項的問題
    - 問題：顯示所有 VariantType 的選項，而非只顯示該商品使用的選項
    - 解決：新增 `usedOptionIds` 過濾邏輯，只顯示商品變體實際使用的選項
    - 結果：顏色只顯示 4 種、尺寸只顯示 S/M/L（無 FREE）
  - 修復缺貨變體無法選擇的問題
    - 問題：`disabled={!isAvailableForSale}` 導致缺貨選項不可點擊
    - 解決：移除 disabled，改用視覺樣式區分缺貨狀態（紅色高亮 + 刪除線）
    - 結果：可選擇缺貨變體，觸發 RestockNotifyButton 顯示
  - 修復 `/api/wishlist` 和 `/api/restock-requests` 認證失敗問題
    - 問題：API 檢查 `authorization` header 存在才執行認證，但前端使用 cookie
    - 解決：移除 authHeader 檢查，讓 `payload.auth()` 自動處理 cookie/bearer token
    - 結果：API 返回 `success: true`，功能正常
  - 設置測試變體：「深藍 + S」庫存設為 0，驗證補貨通知按鈕
- ✅ **Phase 11 商品頁整合 - 願望清單 & 補貨通知**
  - 新增 `WishlistButton` 組件 (`src/components/product/WishlistButton.tsx`)
    - 愛心 Icon 顯示收藏狀態（紅色填滿/空心）
    - 支援登入檢查、變體選擇驗證
    - 整合現有 `/api/wishlist` API 端點
  - 新增 `RestockNotifyButton` 組件 (`src/components/product/RestockNotifyButton.tsx`)
    - 僅在缺貨時顯示「到貨通知我」按鈕
    - 已申請時顯示「已訂閱補貨通知」狀態
    - 整合現有 `/api/restock-requests` API 端點
  - 整合至 `ProductDescription` 組件
    - 愛心 Icon 顯示於商品標題旁
    - 補貨通知按鈕顯示於加入購物車按鈕旁
- ✅ **EasyStore 匯入系統 Bug 修復 + 端對端測試通過**
  - 修復 `processVariants()` 無法解析 EasyStore 實際 API 格式的問題
    - EasyStore API 使用 `variant.name` (如 "白色, FREE") 而非 `variant.option1/2/3`
    - EasyStore API 沒有 `product.options` 陣列
  - 新增 `parseVariantName()` 函數解析變體名稱
  - 新增 `inferOptionTypeName()` 函數自動判斷選項類型（顏色/尺寸）
  - 修復圖片 URL 欄位：EasyStore 使用 `url` 而非 `src`
  - 修復更新商品時重複建立變體的問題（先刪除舊變體）
  - 端對端測試驗證：商品、變體、圖片、圖片-變體關聯全部正常
- ✅ **圖片-變體關聯優化**
  - 修復只有第一張圖有顏色關聯的問題
  - 優化 `processImagesWithVariantLinks()`：優先匯入顏色代表圖
  - 增加 maxImages 上限至 15 張以容納更多顏色
  - 驗證結果：所有顏色都有對應的代表圖片

### 2026-01-30
- ✅ **後台批量操作系統（BulkActions）完整實作**
  - 建立 `src/components/Admin/BulkActions/GenericBulkActions.tsx` 通用元件
    - 支援「選取項目操作」與「選取以外全部操作」雙模式
    - 解決 Payload CMS 原生選取機制的限制（取消勾選後無法正確計算剩餘數量）
    - 支援批量刪除、批量匯出（CSV）、批量狀態變更
    - 可配置色彩主題：amber, blue, emerald, purple, rose
    - 使用 UI/UX Pro Max 設計規範（SaaS Dashboard 色彩、正確的互動回饋）
  - 建立 `src/components/Admin/BulkActions/index.tsx` 預設配置匯出
    - UsersBulkActions - 用戶批量管理
    - OrdersBulkActions - 訂單批量管理（狀態變更支援）
    - MediaBulkActions - 媒體批量管理
    - CategoriesBulkActions - 分類批量管理
    - VendorsBulkActions - 商家批量管理（狀態：待審核/已啟用/已停用）
    - WishlistBulkActions - 願望清單批量管理
    - RestockRequestsBulkActions - 補貨通知批量管理（狀態：等待中/已通知/已取消）
    - PromotionsBulkActions - 促銷活動批量管理（狀態：草稿/啟用/已結束）
  - 更新所有 Collection 配置添加 `beforeListTable` 組件：
    - `src/collections/Users/index.ts`
    - `src/collections/Media.ts`
    - `src/collections/Categories.ts`
    - `src/collections/Vendors/index.ts`
    - `src/collections/Wishlist.ts`
    - `src/collections/RestockRequests.ts`
    - `src/collections/Promotions/index.ts`
    - `src/plugins/index.ts` (Orders via ecommerce plugin)

### 2026-01-26
- ✅ **EasyStore 變體同步功能完整實作**
  - `src/lib/import/easystore-importer.ts` 新增完整變體處理邏輯
  - 新增 `getOrCreateVariantType()` - 建立/查詢規格類型
  - 新增 `getOrCreateVariantOption()` - 建立/查詢規格選項
  - 新增 `createProductVariant()` - 建立產品變體
  - 新增 `processVariants()` - 處理 EasyStore 變體結構
  - 新增 `processImagesWithVariantLinks()` - 圖片變體關聯
- ✅ SSE 串流 API 更新顯示變體同步數量
- ✅ 前端 ProductImporter 介面顯示變體統計

### 2026-01-21
- ✅ **Phase 9 全數完成**
  - 完成 `CartDrawer` 側邊欄重構
  - 完成專屬 `/cart` 購物車頁面
  - 完成新版結帳頁面 UI (`CheckoutPageScrapbook`)
  - 修正免運門檻顯示邏輯
  - 驗證所有 Scrapbook Retro 視覺風格
- 🚧 開始 Phase 9 購物車系統開發
- ✅ SiteSettings 新增「購物車與運費設定」區塊

### 2026-01-16
- ✅ 完成 Phase 8 會員中心全部頁面
- ✅ 推送至 GitHub (commit: df42134)

### 2026-01-15
- ✅ 完成 LINE Bot 整合

- ✅ **BulkActions 延遲問題修復**
  - 問題：點擊刪除按鈕後需等待 10+ 秒才顯示確認對話框
  - 根因：`getDocIds()` API 呼叫在顯示確認對話框「之前」執行
  - 解決方案：
    1. 重新排序流程：先顯示確認對話框（使用預估數量），確認後才取得實際 ID
    2. 優化 `fetchTotal` 查詢：從 `limit=0` 改為 `limit=1&depth=0`（44s → 2.5s）
    3. 優化 `getDocIds` 查詢：使用分頁（500 筆/頁）+ `select=id` 加速

- ✅ **Admin 組件 SCSS 樣式系統建立（UI/UX Pro Max）**
  - 使用 UI/UX Pro Max 設計規範：Data-Dense Dashboard 風格
  - **發現關鍵問題**：Payload Admin 有獨立的 CSS 環境，不載入專案的 Tailwind CSS
  - **解決方案**：使用 SCSS + CSS 變數（定義於 `/src/app/(payload)/custom.scss`）
  - 色彩主題：Primary #C9915D (Copper Bronze), Success #10B981 (Emerald)

  - **ScraperDashboard SCSS 系統** (`src/components/Admin/Scraper/Dashboard/`)
    - 建立 `styles.scss` 使用 CSS 變數
    - 更新 `index.tsx` 使用自訂 class names
    - 統計卡片、連線狀態、工作列表、表格樣式

  - **ProductImporter SCSS 系統** (`src/components/Admin/ProductImporter/`)
    - 建立 `styles.scss`（700+ 行完整樣式）
    - 更新 `index.tsx` 全面使用 SCSS class names
    - 包含：tabs, info-card, card, form-group, checkbox-group, btn variants
    - 包含：stats-grid, progress-section, logs-container, file-upload
    - 包含：data-table, status-badge, empty-state, loading-state
    - 四個 Tab 全部驗證通過：EasyStore、Webhook、CSV、Status

  - **CSS 變數參考** (custom.scss):
    - `--color-primary: #C9915D` (銅棕色)
    - `--color-success: #10B981` (翠綠色)
    - `--radius-sm: 0.5rem`, `--radius-lg: 1rem`
