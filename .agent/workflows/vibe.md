---
description: 啟動 Vibe Coding 核心協議 (Documentation-Driven Development) 與 UI/UX Pro Max 設計引擎
---

1. **核心協議同步 (Core Protocol)**：
   - 讀取並遵循 `view_file(AbsolutePath="/Users/chenyanxiang/.gemini/antigravity/skills/vibe-coding-core/SKILL.md")`。
   - 執行 **Rescue Mode**：讀取 `memory-bank/` 資料夾重建上下文，並在對話框總結 `activeContext.md` 與 `progress.md`。

2. **UI/UX 自動連動 (Design Intelligence)**：
   - **強制觸發**：若任務涉及 **前端開發、UI 組件設計、頁面美化、切版或 UX 優化**，必須立即調閱並執行 `view_file(AbsolutePath="/Users/chenyanxiang/Desktop/網頁開發/.agent/workflows/ui-ux-pro-max.md")`。
   - **設計原則**：
     - 執行設計研究：使用 `search.py` 確定配色 (Color)、字體 (Typography) 與風格 (Style)。
     - 視覺規範：禁用 Emoji Icon，統一使用 SVG (Heroicons/Lucide)；確保具備 Hover 回饋與 Cursor Pointer。
     - 視覺震撼：優先考慮微紋理、玻璃擬態、平滑過渡與高品質配色。

3. **實作與驗證 (Implementation)**：
   - 在 `implementation-plan.md` 中詳細規劃 UI 結構與資料邏輯。
   - 嚴格執行「Vibe Coding 步進式實作」：
     - 執行 **單一** 步驟。
     - 進行 驗證/測試 (使用瀏覽器或代碼校對)。
     - 更新 `progress.md`。
     - 尋求用戶對該步成果的核准，才可進入下一步。

4. **下一步啟動**：
   - 總結完畢後詢問用戶：「準備好按照 [計畫名稱/下一步] 執行實作了嗎？」
